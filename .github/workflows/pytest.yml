name: pytest
on: workflow_dispatch
jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      # - name: clone repository
      #   run: |
      #     git clone https://github.com/LuckyZhangWeiwei/github-actions-python-master.git
      #     cd github-actions-python-master
      - name: checkout code
        uses: actions/checkout@v4
      - name: install pytest
        run: |
          pip install pytest
      - name: run pytest
        run: |
          pytest
